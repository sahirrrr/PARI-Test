.DS_Store
.vscode/
.env
*.env
.vscode
.idea
dump.rdb

# we want to ignore this directory (ends with /)
vendor/

# we want to ignore files under this directory (ends with /*)
.coverage/*

# we want to keep track of this .keep files (starts with !)
!.keep